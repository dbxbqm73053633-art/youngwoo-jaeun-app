export default function MainCtaButtons() {
  // The legacy Home screen has no visible CTA button group yet.
  // Keep this component as the migration boundary without changing the UI.
  return null;
}
