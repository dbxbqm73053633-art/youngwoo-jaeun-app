export default function AnniversarySettings() {
  // TODO: anniversary editing has not existed as a separate admin setting yet.
  // Keep this migration boundary empty so the current UI does not change.
  return null;
}
