export default function BackupSettings() {
  // TODO: backup/export controls are not part of the current legacy admin UI.
  // Rendering nothing preserves the existing settings layout.
  return null;
}
