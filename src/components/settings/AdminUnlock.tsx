export default function AdminUnlock() {
  // TODO: admin access is still handled by the global legacy lock screen.
  // Rendering nothing preserves the current UI and unlock behavior.
  return null;
}
